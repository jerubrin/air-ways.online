export enum Gender {
  Male = 'Male',
  Female = 'Female'
}

export interface PassengersData {
  id: number;
  firstName: string;
  lastName: string;
  // gender: string;
  // dateOfBirth: string;
  // specialAssistance?: boolean;
}
